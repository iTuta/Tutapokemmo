# 微信小程序原生版

`miniprogram/` 现在是原生 WXML/WXSS/JS 页面，不再使用 `web-view`，因此开发者工具模拟器、手机预览和上传版本都不依赖服务器、域名或 HTTPS。

## 导入项目

1. 打开微信开发者工具，导入目录：`miniprogram/`。
2. AppID 使用 `wx9a3e00f392bb871a`。
3. 点击编译即可打开原生查询页。
4. 点击“预览”生成二维码，手机和电脑不需要在同一网络。

原生页已迁移网页端的功能：

- 名称、地区、季节/季节限定、时间段、群怪和闪战分级筛选
- 携带道具搜索：按道具名称（如幸运蛋、护符金币）筛选可携带的精灵，卡片标注命中道具
- 5 群怪努力值筛选和经验升降序
- 地点中英文后缀、纯点、三时段 5%、同地点四季纯点开关
- 进化链联合搜索和树形进化链面板（含进化条件、匹配高亮）
- 宝可梦去重、筛选状态保存和重置
- 5 群怪收益详情：总经验、平均经验（混合群怪）、努力值、成长、携带物
- 平均经验按活跃时段分别计算，卡片展示所有时段中最高的一档并标注时段；
  地点弹窗中每条群怪显示当前时段的经验与 EV 收益
- 宝可梦全部分布详情：统计普通/3群/5群数量、按经验排序、分页
- 点击早/昼/夜打开地点弹窗：可切换晨/昼/夜，按群怪类型筛选（带数量），
  显示完整混群（同一群怪槽位的全部成员及遭遇率），纯点模式下同样可见
- 同一档位存在多组混群（如不归之穴把多层未知区域合并成一个地点名）时，
  按“概率合计≈5%”自动拆组，并用 混群①/② 彩色标题与徽章醒目标注
- 本地分页加载和本地精灵图

## 数据与资源

数据由 `scripts/build-miniprogram-data.js` 从 `web/search-data.js` 和 `web/data/game-data.js` 生成。数据和基础形态精灵图已放入小程序本地包：

```powershell
node scripts/build-miniprogram-data.js
```

如果更新了网页数据，重新运行上面的命令，再在开发者工具中重新编译。

## 旧版 H5

网页入口 `web/index-sprites.html` 仍可单独部署使用，但它依赖浏览器 DOM；需要放入小程序时应使用这里的原生页面，不要再配置 `web-view` 地址。
